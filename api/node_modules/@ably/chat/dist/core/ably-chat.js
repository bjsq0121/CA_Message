import * as l from "ably";
import { ErrorInfo as Ie } from "ably";
var yt = /* @__PURE__ */ ((i) => (i.Trace = "trace", i.Debug = "debug", i.Info = "info", i.Warn = "warn", i.Error = "error", i.Silent = "silent", i))(yt || {});
const Kr = (i, e, t) => {
  const n = t ? `, context: ${JSON.stringify(t)}` : "", s = `[${(/* @__PURE__ */ new Date()).toISOString()}] ${e.valueOf().toUpperCase()} ably-chat: ${i}${n}`;
  switch (e) {
    case "trace":
    case "debug": {
      console.log(s);
      break;
    }
    case "info": {
      console.info(s);
      break;
    }
    case "warn": {
      console.warn(s);
      break;
    }
    case "error": {
      console.error(s);
      break;
    }
  }
}, Qr = (i) => {
  const e = i.logHandler ?? Kr;
  return new Te(e, i.logLevel);
}, lt = /* @__PURE__ */ new Map([
  [
    "trace",
    0
    /* Trace */
  ],
  [
    "debug",
    1
    /* Debug */
  ],
  [
    "info",
    2
    /* Info */
  ],
  [
    "warn",
    3
    /* Warn */
  ],
  [
    "error",
    4
    /* Error */
  ],
  [
    "silent",
    5
    /* Silent */
  ]
]);
class Te {
  constructor(e, t, n) {
    this._handler = e, this._context = n;
    const s = lt.get(t);
    if (s === void 0)
      throw new l.ErrorInfo(`Invalid log level: ${t}`, 5e4, 500);
    this._levelNumber = s;
  }
  trace(e, t) {
    this._write(e, "trace", 0, t);
  }
  debug(e, t) {
    this._write(e, "debug", 1, t);
  }
  info(e, t) {
    this._write(e, "info", 2, t);
  }
  warn(e, t) {
    this._write(e, "warn", 3, t);
  }
  error(e, t) {
    this._write(e, "error", 4, t);
  }
  withContext(e) {
    var n;
    const t = ((n = [...lt.entries()].find(([, s]) => s === this._levelNumber)) == null ? void 0 : n[0]) ?? "error";
    return new Te(this._handler, t, this._mergeContext(e));
  }
  _write(e, t, n, s) {
    n >= this._levelNumber && this._handler(e, t, this._mergeContext(s));
  }
  _mergeContext(e) {
    return this._context ? e ? { ...this._context, ...e } : this._context : e ?? void 0;
  }
}
const Jr = {
  logLevel: yt.Error
}, Xr = (i) => (i = i ?? {}, {
  ...i,
  logLevel: i.logLevel ?? Jr.logLevel
}), Yr = l.Realtime.EventEmitter;
class C extends Yr {
}
const M = (i) => (...e) => i(...e), Zr = 5e3;
var en = /* @__PURE__ */ ((i) => (i.Initialized = "initialized", i.Connecting = "connecting", i.Connected = "connected", i.Disconnected = "disconnected", i.Suspended = "suspended", i.Failed = "failed", i))(en || {});
class tn {
  /**
   * Constructs a new `DefaultConnection` instance.
   * @param ably The Ably Realtime client.
   * @param logger The logger to use.
   */
  constructor(e, t) {
    this._status = "initialized", this._emitter = new C(), this._logger = t, this._status = this._mapAblyStatusToChat(e.connection.state), this._error = e.connection.errorReason, this._connection = e.connection, this._connection.on((n) => {
      const s = this._mapAblyStatusToChat(n.current);
      if (s === this._status)
        return;
      const c = {
        current: s,
        previous: this._status,
        error: n.reason,
        retryIn: n.retryIn
      };
      if (s === "disconnected" && !this._transientTimeout) {
        this._transientTimeout = setTimeout(() => {
          this._onTransientDisconnectTimeout(c);
        }, Zr);
        return;
      }
      if (this._transientTimeout) {
        if (s === "connecting" || s === "disconnected") {
          this._logger.debug("ignoring transient state due to transient disconnect timeout", c);
          return;
        }
        this._cancelTransientDisconnectTimeout();
      }
      this._applyStatusChange(c);
    });
  }
  /**
   * @inheritdoc
   */
  get status() {
    return this._status;
  }
  /**
   * @inheritdoc
   */
  get error() {
    return this._error;
  }
  /**
   * @inheritdoc
   */
  onStatusChange(e) {
    const t = M(e);
    return this._emitter.on(t), {
      off: () => {
        this._emitter.off(t);
      }
    };
  }
  /**
   * @inheritdoc
   */
  offAllStatusChange() {
    this._emitter.off();
  }
  _applyStatusChange(e) {
    this._status = e.current, this._error = e.error, this._logger.info("Connection state changed", e), this._emitter.emit(e.current, e);
  }
  _mapAblyStatusToChat(e) {
    switch (e) {
      case "closing":
      case "closed":
        return "failed";
      default:
        return e;
    }
  }
  /**
   * Handles a transient disconnect timeout.
   *
   * @param statusChange The change in status.
   */
  _onTransientDisconnectTimeout(e) {
    this._logger.debug("transient disconnect timeout reached"), this._cancelTransientDisconnectTimeout(), this._applyStatusChange({
      ...e,
      current: this._mapAblyStatusToChat(this._connection.state)
    });
  }
  /**
   * Cancels the transient disconnect timeout.
   */
  _cancelTransientDisconnectTimeout() {
    this._transientTimeout && (clearTimeout(this._transientTimeout), this._transientTimeout = void 0);
  }
}
var dt = Object.prototype.hasOwnProperty;
function gt(i, e, t) {
  for (t of i.keys())
    if (K(t, e)) return t;
}
function K(i, e) {
  var t, n, s;
  if (i === e) return !0;
  if (i && e && (t = i.constructor) === e.constructor) {
    if (t === Date) return i.getTime() === e.getTime();
    if (t === RegExp) return i.toString() === e.toString();
    if (t === Array) {
      if ((n = i.length) === e.length)
        for (; n-- && K(i[n], e[n]); ) ;
      return n === -1;
    }
    if (t === Set) {
      if (i.size !== e.size)
        return !1;
      for (n of i)
        if (s = n, s && typeof s == "object" && (s = gt(e, s), !s) || !e.has(s)) return !1;
      return !0;
    }
    if (t === Map) {
      if (i.size !== e.size)
        return !1;
      for (n of i)
        if (s = n[0], s && typeof s == "object" && (s = gt(e, s), !s) || !K(n[1], e.get(s)))
          return !1;
      return !0;
    }
    if (t === ArrayBuffer)
      i = new Uint8Array(i), e = new Uint8Array(e);
    else if (t === DataView) {
      if ((n = i.byteLength) === e.byteLength)
        for (; n-- && i.getInt8(n) === e.getInt8(n); ) ;
      return n === -1;
    }
    if (ArrayBuffer.isView(i)) {
      if ((n = i.byteLength) === e.byteLength)
        for (; n-- && i[n] === e[n]; ) ;
      return n === -1;
    }
    if (!t || typeof i == "object") {
      n = 0;
      for (t in i)
        if (dt.call(i, t) && ++n && !dt.call(e, t) || !(t in e) || !K(i[t], e[t])) return !1;
      return Object.keys(e).length === n;
    }
  }
  return i !== i && e !== e;
}
var P = /* @__PURE__ */ ((i) => (i.Created = "message.created", i.Updated = "message.updated", i.Deleted = "message.deleted", i))(P || {}), bt = /* @__PURE__ */ ((i) => (i.ChatMessage = "chat.message", i))(bt || {}), v = /* @__PURE__ */ ((i) => (i.MessageCreate = "message.create", i.MessageUpdate = "message.update", i.MessageDelete = "message.delete", i.MessageAnnotationSummary = "message.summary", i.MessageMetaOccupancy = "meta.occupancy", i))(v || {}), rn = /* @__PURE__ */ ((i) => (i.Enter = "enter", i.Leave = "leave", i.Update = "update", i.Present = "present", i))(rn || {}), T = /* @__PURE__ */ ((i) => (i.Start = "typing.started", i.Stop = "typing.stopped", i))(T || {}), q = /* @__PURE__ */ ((i) => (i.SetChanged = "typing.set.changed", i))(q || {}), se = /* @__PURE__ */ ((i) => (i.Reaction = "roomReaction", i))(se || {}), S = /* @__PURE__ */ ((i) => (i.Unique = "reaction:unique.v1", i.Distinct = "reaction:distinct.v1", i.Multiple = "reaction:multiple.v1", i))(S || {}), R = /* @__PURE__ */ ((i) => (i.Create = "reaction.create", i.Delete = "reaction.delete", i.Summary = "reaction.summary", i))(R || {}), W = /* @__PURE__ */ ((i) => (i.Discontinuity = "room.discontinuity", i))(W || {});
class A {
  constructor({
    serial: e,
    clientId: t,
    roomId: n,
    text: s,
    metadata: c,
    headers: u,
    action: d,
    version: f,
    createdAt: p,
    timestamp: _,
    reactions: b,
    operation: j
  }) {
    this.serial = e, this.clientId = t, this.roomId = n, this.text = s, this.metadata = c, this.headers = u, this.action = d, this.version = f, this.createdAt = p, this.timestamp = _, this.reactions = b, this.operation = j, Object.freeze(this.reactions), Object.freeze(this.reactions.multiple), Object.freeze(this.reactions.distinct), Object.freeze(this.reactions.unique), Object.freeze(this);
  }
  get isUpdated() {
    return this.action === v.MessageUpdate;
  }
  get isDeleted() {
    return this.action === v.MessageDelete;
  }
  get updatedBy() {
    var e;
    return this.isUpdated ? (e = this.operation) == null ? void 0 : e.clientId : void 0;
  }
  get deletedBy() {
    var e;
    return this.isDeleted ? (e = this.operation) == null ? void 0 : e.clientId : void 0;
  }
  get updatedAt() {
    return this.isUpdated ? this.timestamp : void 0;
  }
  get deletedAt() {
    return this.isDeleted ? this.timestamp : void 0;
  }
  isOlderVersionOf(e) {
    return this.equal(e) ? this.version < e.version : !1;
  }
  isNewerVersionOf(e) {
    return this.equal(e) ? this.version > e.version : !1;
  }
  isSameVersionAs(e) {
    return this.equal(e) ? this.version === e.version : !1;
  }
  before(e) {
    return this.serial < e.serial;
  }
  after(e) {
    return this.serial > e.serial;
  }
  equal(e) {
    return this.serial === e.serial;
  }
  isSameAs(e) {
    return this.equal(e);
  }
  with(e) {
    if (e.type === P.Created)
      throw new Ie("cannot apply a created event to a message", 4e4, 400);
    if (e.type === R.Summary) {
      if (e.summary.messageSerial !== this.serial)
        throw new Ie("cannot apply event for a different message", 4e4, 400);
      const t = {
        unique: structuredClone(e.summary.unique),
        distinct: structuredClone(e.summary.distinct),
        multiple: structuredClone(e.summary.multiple)
      };
      return A._clone(this, { reactions: t });
    }
    if (e.message.serial !== this.serial)
      throw new Ie("cannot apply event for a different message", 4e4, 400);
    return this.version >= e.message.version ? this : A._clone(e.message, { reactions: this.reactions });
  }
  // Clone a message, optionally replace the given fields
  static _clone(e, t) {
    return new A({
      serial: (t == null ? void 0 : t.serial) ?? e.serial,
      clientId: (t == null ? void 0 : t.clientId) ?? e.clientId,
      roomId: (t == null ? void 0 : t.roomId) ?? e.roomId,
      text: (t == null ? void 0 : t.text) ?? e.text,
      metadata: (t == null ? void 0 : t.metadata) ?? structuredClone(e.metadata),
      headers: (t == null ? void 0 : t.headers) ?? structuredClone(e.headers),
      action: (t == null ? void 0 : t.action) ?? e.action,
      version: (t == null ? void 0 : t.version) ?? e.version,
      createdAt: (t == null ? void 0 : t.createdAt) ?? e.createdAt,
      timestamp: (t == null ? void 0 : t.timestamp) ?? e.timestamp,
      reactions: (t == null ? void 0 : t.reactions) ?? structuredClone(e.reactions),
      operation: (t == null ? void 0 : t.operation) ?? structuredClone(e.operation)
    });
  }
  copy(e = {}) {
    return A._clone(this, e);
  }
}
function Q() {
  return {
    unique: {},
    distinct: {},
    multiple: {}
  };
}
function nn(i, e) {
  const t = e;
  if (!i)
    throw new l.ErrorInfo("received incoming message without roomId", 5e4, 500);
  if (!t.data)
    throw new l.ErrorInfo("received incoming message without data", 5e4, 500);
  if (!t.clientId)
    throw new l.ErrorInfo("received incoming message without clientId", 5e4, 500);
  if (t.data.text === void 0)
    throw new l.ErrorInfo("received incoming message without text", 5e4, 500);
  if (!t.extras)
    throw new l.ErrorInfo("received incoming message without extras", 5e4, 500);
  if (!t.serial)
    throw new l.ErrorInfo("received incoming message without serial", 5e4, 500);
  if (!t.version)
    throw new l.ErrorInfo("received incoming message without version", 5e4, 500);
  if (!t.createdAt)
    throw new l.ErrorInfo("received incoming message without createdAt", 5e4, 500);
  if (!t.timestamp)
    throw new l.ErrorInfo("received incoming message without timestamp", 5e4, 500);
  switch (t.action) {
    case v.MessageCreate:
    case v.MessageUpdate:
    case v.MessageDelete:
      break;
    default:
      throw new l.ErrorInfo(`received incoming message with unhandled action; ${t.action}`, 5e4, 500);
  }
  return new A({
    serial: t.serial,
    clientId: t.clientId,
    roomId: i,
    text: t.data.text,
    metadata: t.data.metadata ?? {},
    headers: t.extras.headers ?? {},
    action: t.action,
    version: t.version,
    createdAt: new Date(t.createdAt),
    timestamp: new Date(t.timestamp),
    reactions: Q(),
    operation: t.operation
  });
}
class wt {
  constructor(e, t, n, s, c) {
    var u, d;
    this._logger = e, this._options = t, this._api = n, this._roomID = s, this._channel = c, this._emitter = new C(), c.subscribe(this._processMessageEvent.bind(this)), (u = this._options) != null && u.rawMessageReactions && c.annotations.subscribe(this._processAnnotationEvent.bind(this)), this._defaultType = ((d = this._options) == null ? void 0 : d.defaultMessageReactionType) ?? S.Distinct;
  }
  _processAnnotationEvent(e) {
    if (this._logger.trace("MessagesReactions._processAnnotationEvent();", { event: e }), !e.messageSerial) {
      this._logger.warn(
        "DefaultMessageReactions._processAnnotationEvent(); received event with missing messageSerial",
        {
          event: e
        }
      );
      return;
    }
    if (!Object.values(S).includes(e.type)) {
      this._logger.debug("DefaultMessageReactions._processAnnotationEvent(); received event with unknown type", {
        event: e
      });
      return;
    }
    const t = e.type, s = {
      "annotation.create": R.Create,
      "annotation.delete": R.Delete
    }[e.action];
    if (!s) {
      this._logger.warn("DefaultMessageReactions._processAnnotationEvent(); received event with unknown action", {
        event: e
      });
      return;
    }
    let c = e.name;
    if (!c)
      if (s === R.Delete && t === S.Unique)
        c = "";
      else
        return;
    const u = {
      type: s,
      timestamp: new Date(e.timestamp),
      reaction: {
        messageSerial: e.messageSerial,
        type: t,
        name: c,
        clientId: e.clientId ?? ""
      }
    };
    e.count ? u.reaction.count = e.count : s === R.Create && t === S.Multiple && (u.reaction.count = 1), this._emitter.emit(s, u);
  }
  _processMessageEvent(e) {
    if (this._logger.trace("MessagesReactions._processMessageEvent();", { event: e }), e.action !== v.MessageAnnotationSummary)
      return;
    if (e.summary || (e.summary = {}), !e.serial) {
      this._logger.warn("DefaultMessageReactions._processMessageEvent(); received summary without serial", {
        event: e
      });
      return;
    }
    const t = e.summary[S.Unique] ?? {}, n = e.summary[S.Distinct] ?? {}, s = e.summary[S.Multiple] ?? {};
    this._emitter.emit(R.Summary, {
      type: R.Summary,
      summary: {
        messageSerial: e.serial,
        unique: t,
        distinct: n,
        multiple: s
      }
    });
  }
  /**
   * @inheritDoc
   */
  add(e, t) {
    this._logger.trace("MessagesReactions.add();", { message: e, params: t });
    let { type: n, count: s } = t;
    n || (n = this._defaultType), n === S.Multiple && !s && (s = 1);
    const c = { type: n, name: t.name };
    return s && (c.count = s), this._api.addMessageReaction(this._roomID, e.serial, c);
  }
  /**
   * @inheritDoc
   */
  delete(e, t) {
    this._logger.trace("MessagesReactions.delete();", { message: e, params: t });
    let n = t == null ? void 0 : t.type;
    if (n || (n = this._defaultType), n !== S.Unique && !(t != null && t.name))
      throw new l.ErrorInfo(`cannot delete reaction of type ${n} without a name`, 40001, 400);
    const s = { type: n };
    return n !== S.Unique && (s.name = t == null ? void 0 : t.name), this._api.deleteMessageReaction(this._roomID, e.serial, s);
  }
  /**
   * @inheritDoc
   */
  subscribe(e) {
    this._logger.trace("MessagesReactions.subscribe();");
    const t = M(e);
    return this._emitter.on(R.Summary, t), {
      unsubscribe: () => {
        this._emitter.off(t);
      }
    };
  }
  /**
   * @inheritDoc
   */
  subscribeRaw(e) {
    var n;
    if (this._logger.trace("MessagesReactions.subscribeRaw();"), !((n = this._options) != null && n.rawMessageReactions))
      throw new l.ErrorInfo("Raw message reactions are not enabled", 40001, 400);
    const t = M(e);
    return this._emitter.on([R.Create, R.Delete], t), {
      unsubscribe: () => {
        this._emitter.off(t);
      }
    };
  }
  /**
   * Merges the channel options to add support for message reactions.
   *
   * @param roomOptions The room options to merge for.
   * @returns A function that merges the channel options for the room with the ones required for presence.
   */
  static channelOptionMerger(e) {
    return (t) => (t.modes.includes("ANNOTATION_PUBLISH") || t.modes.push("ANNOTATION_PUBLISH"), e.messages.rawMessageReactions && !t.modes.includes("ANNOTATION_SUBSCRIBE") && t.modes.push("ANNOTATION_SUBSCRIBE"), t);
  }
}
const sn = /* @__PURE__ */ new Map([
  [v.MessageCreate, P.Created],
  [v.MessageUpdate, P.Updated],
  [v.MessageDelete, P.Deleted]
]);
var ve = /* @__PURE__ */ ((i) => (i.OldestFirst = "oldestFirst", i.NewestFirst = "newestFirst", i))(ve || {});
class an {
  /**
   * Constructs a new `DefaultMessages` instance.
   * @param roomId The unique identifier of the room.
   * @param channel An instance of the Realtime channel for the room.
   * @param chatApi An instance of the ChatApi.
   * @param clientId The client ID of the user.
   * @param logger An instance of the Logger.
   */
  constructor(e, t, n, s, c, u) {
    this._emitter = new C(), this._roomId = e, this._options = t, this._channel = n, this._chatApi = s, this._clientId = c, this._logger = u, this._listenerSubscriptionPoints = /* @__PURE__ */ new Map(), this.reactions = new wt(this._logger, t, this._chatApi, this._roomId, this._channel), this._applyChannelSubscriptions();
  }
  /**
   * Sets up channel subscriptions for messages.
   */
  _applyChannelSubscriptions() {
    this._channel.subscribe([bt.ChatMessage], this._processEvent.bind(this)), this._channel.on("attached", (e) => {
      this._handleAttach(e.resumed);
    }), this._channel.on("update", (e) => {
      e.current === "attached" && e.previous === "attached" && this._handleAttach(e.resumed);
    });
  }
  /**
   * @inheritdoc Messages
   */
  async _getBeforeSubscriptionStart(e, t) {
    this._logger.trace("DefaultSubscriptionManager.getBeforeSubscriptionStart();");
    const n = this._listenerSubscriptionPoints.get(e);
    if (n === void 0)
      throw this._logger.error(
        "DefaultSubscriptionManager.getBeforeSubscriptionStart(); listener has not been subscribed yet"
      ), new l.ErrorInfo(
        "cannot query history; listener has not been subscribed yet",
        4e4,
        400
      );
    const s = await n;
    return this._chatApi.getMessages(this._roomId, {
      ...t,
      orderBy: "newestFirst",
      ...s
    });
  }
  /**
   * Handle the case where the channel experiences a detach and reattaches.
   */
  _handleAttach(e) {
    if (this._logger.trace("DefaultSubscriptionManager.handleAttach();"), e) return;
    const t = this._subscribeAtChannelAttach();
    for (const [n] of this._listenerSubscriptionPoints.entries())
      this._listenerSubscriptionPoints.set(n, t);
  }
  /**
   * Create a promise that resolves with the attachSerial of the channel or the serial of the latest message.
   */
  async _resolveSubscriptionStart() {
    const e = this._getChannelProperties();
    if (e.state === "attached") {
      if (e.properties.channelSerial)
        return { fromSerial: e.properties.channelSerial };
      throw this._logger.error("DefaultSubscriptionManager.handleAttach(); channelSerial is undefined"), new l.ErrorInfo("channel is attached, but channelSerial is not defined", 4e4, 400);
    }
    return this._subscribeAtChannelAttach();
  }
  _getChannelProperties() {
    return this._channel;
  }
  async _subscribeAtChannelAttach() {
    const e = this._getChannelProperties();
    return new Promise((t, n) => {
      e.state === "attached" && (this._logger.debug("Messages._subscribeAtChannelAttach(); channel is attached already, using attachSerial", {
        attachSerial: e.properties.attachSerial
      }), e.properties.attachSerial ? t({ fromSerial: e.properties.attachSerial }) : (this._logger.error("DefaultSubscriptionManager.handleAttach(); attachSerial is undefined"), n(
        new l.ErrorInfo("channel is attached, but attachSerial is not defined", 4e4, 400)
      ))), e.once("attached", () => {
        this._logger.debug("Messages._subscribeAtChannelAttach(); channel is now attached, using attachSerial", {
          attachSerial: e.properties.attachSerial
        }), e.properties.attachSerial ? t({ fromSerial: e.properties.attachSerial }) : (this._logger.error("DefaultSubscriptionManager.handleAttach(); attachSerial is undefined"), n(
          new l.ErrorInfo("channel is attached, but attachSerial is not defined", 4e4, 400)
        ));
      });
    });
  }
  /**
   * @inheritdoc Messages
   */
  async get(e) {
    return this._logger.trace("Messages.query();"), this._chatApi.getMessages(this._roomId, e);
  }
  /**
   * @inheritdoc Messages
   */
  async send(e) {
    this._logger.trace("Messages.send();", { params: e });
    const { text: t, metadata: n, headers: s } = e, c = await this._chatApi.sendMessage(this._roomId, { text: t, headers: s, metadata: n });
    return new A({
      serial: c.serial,
      clientId: this._clientId,
      roomId: this._roomId,
      text: t,
      metadata: n ?? {},
      headers: s ?? {},
      action: v.MessageCreate,
      version: c.serial,
      createdAt: new Date(c.createdAt),
      timestamp: new Date(c.createdAt),
      // timestamp is the same as createdAt for new messages
      reactions: Q()
    });
  }
  async update(e, t) {
    this._logger.trace("Messages.update();", { message: e, details: t });
    const n = await this._chatApi.updateMessage(this._roomId, e.serial, {
      message: {
        text: e.text,
        metadata: e.metadata,
        headers: e.headers
      },
      ...t
    }), s = new A({
      serial: e.serial,
      clientId: e.clientId,
      roomId: this._roomId,
      text: e.text,
      metadata: e.metadata,
      headers: e.headers,
      action: v.MessageUpdate,
      version: n.version,
      createdAt: new Date(e.createdAt),
      timestamp: new Date(n.timestamp),
      reactions: Q(),
      operation: {
        clientId: this._clientId,
        description: t == null ? void 0 : t.description,
        metadata: t == null ? void 0 : t.metadata
      }
    });
    return this._logger.debug("Messages.update(); message update successfully", { message: e }), s;
  }
  /**
   * @inheritdoc Messages
   */
  async delete(e, t) {
    this._logger.trace("Messages.delete();", { params: t });
    const n = await this._chatApi.deleteMessage(this._roomId, e.serial, t), s = new A({
      serial: e.serial,
      clientId: e.clientId,
      roomId: this._roomId,
      text: e.text,
      metadata: e.metadata,
      headers: e.headers,
      action: v.MessageDelete,
      version: n.version,
      createdAt: new Date(e.createdAt),
      timestamp: new Date(n.timestamp),
      reactions: Q(),
      operation: {
        clientId: this._clientId,
        description: t == null ? void 0 : t.description,
        metadata: t == null ? void 0 : t.metadata
      }
    });
    return this._logger.debug("Messages.delete(); message deleted successfully", { deletedMessage: s }), s;
  }
  /**
   * @inheritdoc Messages
   */
  subscribe(e) {
    this._logger.trace("Messages.subscribe();");
    const t = M(e);
    this._emitter.on([P.Created, P.Updated, P.Deleted], t);
    const n = this._resolveSubscriptionStart();
    return n.catch(() => {
      this._logger.debug("Messages.subscribe(); subscription point was not resolved before the room was released");
    }), this._listenerSubscriptionPoints.set(t, n), {
      unsubscribe: () => {
        this._listenerSubscriptionPoints.delete(t), this._logger.trace("Messages.unsubscribe();"), this._emitter.off(t);
      },
      getPreviousMessages: (s) => this._getBeforeSubscriptionStart(t, s)
    };
  }
  /**
   * @inheritdoc Messages
   */
  unsubscribeAll() {
    this._logger.trace("Messages.unsubscribeAll();"), this._emitter.off(), this._listenerSubscriptionPoints.clear();
  }
  _processEvent(e) {
    this._logger.trace("Messages._processEvent();", {
      channelEventMessage: e
    });
    const { action: t } = e, n = sn.get(t);
    if (!n) {
      this._logger.debug("Messages._processEvent(); received unknown message action", { action: t });
      return;
    }
    const s = this._parseNewMessage(e);
    s && this._emitter.emit(n, { type: n, message: s });
  }
  /**
   * Validate the realtime message and convert it to a chat message.
   */
  _parseNewMessage(e) {
    try {
      return nn(this._roomId, e);
    } catch (t) {
      this._logger.error("failed to parse incoming message;", { channelEventMessage: e, error: t });
    }
  }
}
class on {
  constructor(e, t) {
    this._apiProtocolVersion = 3, this._realtime = e, this._logger = t;
  }
  async getMessages(e, t) {
    e = encodeURIComponent(e);
    const n = { ...t };
    if (t.orderBy)
      switch (t.orderBy) {
        case ve.NewestFirst: {
          n.direction = "backwards";
          break;
        }
        case ve.OldestFirst: {
          n.direction = "forwards";
          break;
        }
        default:
          throw new l.ErrorInfo(`invalid orderBy value: ${t.orderBy}`, 4e4, 400);
      }
    const s = await this._makeAuthorizedPaginatedRequest(`/chat/v3/rooms/${e}/messages`, n);
    return this._recursivePaginateMessages(s);
  }
  _recursivePaginateMessages(e) {
    const t = (s) => {
      const c = s.metadata, u = s.headers, d = s.reactions;
      return new A({
        ...s,
        metadata: c ?? {},
        headers: u ?? {},
        createdAt: s.createdAt ? new Date(s.createdAt) : new Date(s.timestamp),
        timestamp: new Date(s.timestamp),
        reactions: d ?? Q()
      });
    }, n = {};
    return n.items = e.items.map((s) => t(s)), n.next = () => e.next().then((s) => s ? this._recursivePaginateMessages(s) : null), n.first = () => e.first().then((s) => this._recursivePaginateMessages(s)), n.current = () => e.current().then((s) => this._recursivePaginateMessages(s)), n.hasNext = () => e.hasNext(), n.isLast = () => e.isLast(), { ...e, ...n };
  }
  deleteMessage(e, t, n) {
    const s = {
      description: n == null ? void 0 : n.description,
      metadata: n == null ? void 0 : n.metadata
    };
    return t = encodeURIComponent(t), e = encodeURIComponent(e), this._makeAuthorizedRequest(
      `/chat/v3/rooms/${e}/messages/${t}/delete`,
      "POST",
      s,
      {}
    );
  }
  sendMessage(e, t) {
    const n = { text: t.text };
    return t.metadata && (n.metadata = t.metadata), t.headers && (n.headers = t.headers), e = encodeURIComponent(e), this._makeAuthorizedRequest(`/chat/v3/rooms/${e}/messages`, "POST", n);
  }
  updateMessage(e, t, n) {
    const s = encodeURIComponent(t);
    return e = encodeURIComponent(e), this._makeAuthorizedRequest(
      `/chat/v3/rooms/${e}/messages/${s}`,
      "PUT",
      n
    );
  }
  addMessageReaction(e, t, n) {
    const s = encodeURIComponent(t);
    return e = encodeURIComponent(e), this._makeAuthorizedRequest(`/chat/v3/rooms/${e}/messages/${s}/reactions`, "POST", n);
  }
  deleteMessageReaction(e, t, n) {
    const s = encodeURIComponent(t);
    return e = encodeURIComponent(e), this._makeAuthorizedRequest(
      `/chat/v3/rooms/${e}/messages/${s}/reactions`,
      "DELETE",
      void 0,
      n
    );
  }
  getOccupancy(e) {
    return e = encodeURIComponent(e), this._makeAuthorizedRequest(`/chat/v3/rooms/${e}/occupancy`, "GET");
  }
  async _makeAuthorizedRequest(e, t, n, s) {
    const c = await this._realtime.request(t, e, this._apiProtocolVersion, s, n);
    if (!c.success)
      throw this._logger.error("ChatApi._makeAuthorizedRequest(); failed to make request", {
        url: e,
        statusCode: c.statusCode,
        errorCode: c.errorCode,
        errorMessage: c.errorMessage
      }), new l.ErrorInfo(c.errorMessage, c.errorCode, c.statusCode);
    return c.items[0];
  }
  async _makeAuthorizedPaginatedRequest(e, t, n) {
    const s = await this._realtime.request("GET", e, this._apiProtocolVersion, t, n);
    if (!s.success)
      throw this._logger.error("ChatApi._makeAuthorizedPaginatedRequest(); failed to make request", {
        url: e,
        statusCode: s.statusCode,
        errorCode: s.errorCode,
        errorMessage: s.errorMessage
      }), new l.ErrorInfo(s.errorMessage, s.errorCode, s.statusCode);
    return s;
  }
}
var U = /* @__PURE__ */ ((i) => (i[i.BadRequest = 4e4] = "BadRequest", i[i.MessageRejectedByBeforePublishRule = 42211] = "MessageRejectedByBeforePublishRule", i[i.MessageRejectedByModeration = 42213] = "MessageRejectedByModeration", i[i.RoomDiscontinuity = 102100] = "RoomDiscontinuity", i[i.RoomInFailedState = 102101] = "RoomInFailedState", i[i.RoomIsReleasing = 102102] = "RoomIsReleasing", i[i.RoomIsReleased = 102103] = "RoomIsReleased", i[i.RoomReleasedBeforeOperationCompleted = 102106] = "RoomReleasedBeforeOperationCompleted", i))(U || {});
const Bn = (i, e) => i.code === e, cn = () => Math.random().toString(36).slice(2);
var ie = typeof globalThis < "u" ? globalThis : typeof window < "u" ? window : typeof global < "u" ? global : typeof self < "u" ? self : {};
function hn(i) {
  return i && i.__esModule && Object.prototype.hasOwnProperty.call(i, "default") ? i.default : i;
}
var V = { exports: {} };
V.exports;
var _t;
function un() {
  return _t || (_t = 1, function(i, e) {
    var t = 200, n = "__lodash_hash_undefined__", s = 9007199254740991, c = "[object Arguments]", u = "[object Array]", d = "[object Boolean]", f = "[object Date]", p = "[object Error]", _ = "[object Function]", b = "[object GeneratorFunction]", j = "[object Map]", Re = "[object Number]", ce = "[object Object]", Me = "[object Promise]", Ae = "[object RegExp]", J = "[object Set]", Ce = "[object String]", Ee = "[object Symbol]", he = "[object WeakMap]", Oe = "[object ArrayBuffer]", X = "[object DataView]", De = "[object Float32Array]", xe = "[object Float64Array]", Pe = "[object Int8Array]", Le = "[object Int16Array]", Ne = "[object Int32Array]", ke = "[object Uint8Array]", qe = "[object Uint8ClampedArray]", Ue = "[object Uint16Array]", je = "[object Uint32Array]", Tt = /[\\^$.*+?()[\]{}|]/g, Rt = /\w*$/, Mt = /^\[object .+?Constructor\]$/, At = /^(?:0|[1-9]\d*)$/, g = {};
    g[c] = g[u] = g[Oe] = g[X] = g[d] = g[f] = g[De] = g[xe] = g[Pe] = g[Le] = g[Ne] = g[j] = g[Re] = g[ce] = g[Ae] = g[J] = g[Ce] = g[Ee] = g[ke] = g[qe] = g[Ue] = g[je] = !0, g[p] = g[_] = g[he] = !1;
    var Ct = typeof ie == "object" && ie && ie.Object === Object && ie, Et = typeof self == "object" && self && self.Object === Object && self, E = Ct || Et || Function("return this")(), ze = e && !e.nodeType && e, Be = ze && !0 && i && !i.nodeType && i, Ot = Be && Be.exports === ze;
    function Dt(r, a) {
      return r.set(a[0], a[1]), r;
    }
    function xt(r, a) {
      return r.add(a), r;
    }
    function Pt(r, a) {
      for (var o = -1, h = r ? r.length : 0; ++o < h && a(r[o], o, r) !== !1; )
        ;
      return r;
    }
    function Lt(r, a) {
      for (var o = -1, h = a.length, y = r.length; ++o < h; )
        r[y + o] = a[o];
      return r;
    }
    function $e(r, a, o, h) {
      for (var y = -1, w = r ? r.length : 0; ++y < w; )
        o = a(o, r[y], y, r);
      return o;
    }
    function Nt(r, a) {
      for (var o = -1, h = Array(r); ++o < r; )
        h[o] = a(o);
      return h;
    }
    function kt(r, a) {
      return r == null ? void 0 : r[a];
    }
    function Fe(r) {
      var a = !1;
      if (r != null && typeof r.toString != "function")
        try {
          a = !!(r + "");
        } catch {
        }
      return a;
    }
    function He(r) {
      var a = -1, o = Array(r.size);
      return r.forEach(function(h, y) {
        o[++a] = [y, h];
      }), o;
    }
    function ue(r, a) {
      return function(o) {
        return r(a(o));
      };
    }
    function Ge(r) {
      var a = -1, o = Array(r.size);
      return r.forEach(function(h) {
        o[++a] = h;
      }), o;
    }
    var qt = Array.prototype, Ut = Function.prototype, Y = Object.prototype, le = E["__core-js_shared__"], We = function() {
      var r = /[^.]+$/.exec(le && le.keys && le.keys.IE_PROTO || "");
      return r ? "Symbol(src)_1." + r : "";
    }(), Ve = Ut.toString, x = Y.hasOwnProperty, Z = Y.toString, jt = RegExp(
      "^" + Ve.call(x).replace(Tt, "\\$&").replace(/hasOwnProperty|(function).*?(?=\\\()| for .+?(?=\\\])/g, "$1.*?") + "$"
    ), Ke = Ot ? E.Buffer : void 0, Qe = E.Symbol, Je = E.Uint8Array, zt = ue(Object.getPrototypeOf, Object), Bt = Object.create, $t = Y.propertyIsEnumerable, Ft = qt.splice, Xe = Object.getOwnPropertySymbols, Ht = Ke ? Ke.isBuffer : void 0, Gt = ue(Object.keys, Object), de = $(E, "DataView"), H = $(E, "Map"), ge = $(E, "Promise"), _e = $(E, "Set"), fe = $(E, "WeakMap"), G = $(Object, "create"), Wt = k(de), Vt = k(H), Kt = k(ge), Qt = k(_e), Jt = k(fe), Ye = Qe ? Qe.prototype : void 0, Ze = Ye ? Ye.valueOf : void 0;
    function L(r) {
      var a = -1, o = r ? r.length : 0;
      for (this.clear(); ++a < o; ) {
        var h = r[a];
        this.set(h[0], h[1]);
      }
    }
    function Xt() {
      this.__data__ = G ? G(null) : {};
    }
    function Yt(r) {
      return this.has(r) && delete this.__data__[r];
    }
    function Zt(r) {
      var a = this.__data__;
      if (G) {
        var o = a[r];
        return o === n ? void 0 : o;
      }
      return x.call(a, r) ? a[r] : void 0;
    }
    function er(r) {
      var a = this.__data__;
      return G ? a[r] !== void 0 : x.call(a, r);
    }
    function tr(r, a) {
      var o = this.__data__;
      return o[r] = G && a === void 0 ? n : a, this;
    }
    L.prototype.clear = Xt, L.prototype.delete = Yt, L.prototype.get = Zt, L.prototype.has = er, L.prototype.set = tr;
    function O(r) {
      var a = -1, o = r ? r.length : 0;
      for (this.clear(); ++a < o; ) {
        var h = r[a];
        this.set(h[0], h[1]);
      }
    }
    function rr() {
      this.__data__ = [];
    }
    function nr(r) {
      var a = this.__data__, o = ee(a, r);
      if (o < 0)
        return !1;
      var h = a.length - 1;
      return o == h ? a.pop() : Ft.call(a, o, 1), !0;
    }
    function ir(r) {
      var a = this.__data__, o = ee(a, r);
      return o < 0 ? void 0 : a[o][1];
    }
    function sr(r) {
      return ee(this.__data__, r) > -1;
    }
    function ar(r, a) {
      var o = this.__data__, h = ee(o, r);
      return h < 0 ? o.push([r, a]) : o[h][1] = a, this;
    }
    O.prototype.clear = rr, O.prototype.delete = nr, O.prototype.get = ir, O.prototype.has = sr, O.prototype.set = ar;
    function z(r) {
      var a = -1, o = r ? r.length : 0;
      for (this.clear(); ++a < o; ) {
        var h = r[a];
        this.set(h[0], h[1]);
      }
    }
    function or() {
      this.__data__ = {
        hash: new L(),
        map: new (H || O)(),
        string: new L()
      };
    }
    function cr(r) {
      return te(this, r).delete(r);
    }
    function hr(r) {
      return te(this, r).get(r);
    }
    function ur(r) {
      return te(this, r).has(r);
    }
    function lr(r, a) {
      return te(this, r).set(r, a), this;
    }
    z.prototype.clear = or, z.prototype.delete = cr, z.prototype.get = hr, z.prototype.has = ur, z.prototype.set = lr;
    function B(r) {
      this.__data__ = new O(r);
    }
    function dr() {
      this.__data__ = new O();
    }
    function gr(r) {
      return this.__data__.delete(r);
    }
    function _r(r) {
      return this.__data__.get(r);
    }
    function fr(r) {
      return this.__data__.has(r);
    }
    function mr(r, a) {
      var o = this.__data__;
      if (o instanceof O) {
        var h = o.__data__;
        if (!H || h.length < t - 1)
          return h.push([r, a]), this;
        o = this.__data__ = new z(h);
      }
      return o.set(r, a), this;
    }
    B.prototype.clear = dr, B.prototype.delete = gr, B.prototype.get = _r, B.prototype.has = fr, B.prototype.set = mr;
    function pr(r, a) {
      var o = ye(r) || Br(r) ? Nt(r.length, String) : [], h = o.length, y = !!h;
      for (var w in r)
        x.call(r, w) && !(y && (w == "length" || qr(w, h))) && o.push(w);
      return o;
    }
    function et(r, a, o) {
      var h = r[a];
      (!(x.call(r, a) && it(h, o)) || o === void 0 && !(a in r)) && (r[a] = o);
    }
    function ee(r, a) {
      for (var o = r.length; o--; )
        if (it(r[o][0], a))
          return o;
      return -1;
    }
    function yr(r, a) {
      return r && tt(a, be(a), r);
    }
    function me(r, a, o, h, y, w, D) {
      var I;
      if (h && (I = w ? h(r, y, w, D) : h(r)), I !== void 0)
        return I;
      if (!re(r))
        return r;
      var ot = ye(r);
      if (ot) {
        if (I = Lr(r), !a)
          return Dr(r, I);
      } else {
        var F = N(r), ct = F == _ || F == b;
        if (Fr(r))
          return Tr(r, a);
        if (F == ce || F == c || ct && !w) {
          if (Fe(r))
            return w ? r : {};
          if (I = Nr(ct ? {} : r), !a)
            return xr(r, yr(I, r));
        } else {
          if (!g[F])
            return w ? r : {};
          I = kr(r, F, me, a);
        }
      }
      D || (D = new B());
      var ht = D.get(r);
      if (ht)
        return ht;
      if (D.set(r, I), !ot)
        var ut = o ? Pr(r) : be(r);
      return Pt(ut || r, function(we, ne) {
        ut && (ne = we, we = r[ne]), et(I, ne, me(we, a, o, h, ne, r, D));
      }), I;
    }
    function br(r) {
      return re(r) ? Bt(r) : {};
    }
    function wr(r, a, o) {
      var h = a(r);
      return ye(r) ? h : Lt(h, o(r));
    }
    function Ir(r) {
      return Z.call(r);
    }
    function vr(r) {
      if (!re(r) || jr(r))
        return !1;
      var a = at(r) || Fe(r) ? jt : Mt;
      return a.test(k(r));
    }
    function Sr(r) {
      if (!nt(r))
        return Gt(r);
      var a = [];
      for (var o in Object(r))
        x.call(r, o) && o != "constructor" && a.push(o);
      return a;
    }
    function Tr(r, a) {
      if (a)
        return r.slice();
      var o = new r.constructor(r.length);
      return r.copy(o), o;
    }
    function pe(r) {
      var a = new r.constructor(r.byteLength);
      return new Je(a).set(new Je(r)), a;
    }
    function Rr(r, a) {
      var o = a ? pe(r.buffer) : r.buffer;
      return new r.constructor(o, r.byteOffset, r.byteLength);
    }
    function Mr(r, a, o) {
      var h = a ? o(He(r), !0) : He(r);
      return $e(h, Dt, new r.constructor());
    }
    function Ar(r) {
      var a = new r.constructor(r.source, Rt.exec(r));
      return a.lastIndex = r.lastIndex, a;
    }
    function Cr(r, a, o) {
      var h = a ? o(Ge(r), !0) : Ge(r);
      return $e(h, xt, new r.constructor());
    }
    function Er(r) {
      return Ze ? Object(Ze.call(r)) : {};
    }
    function Or(r, a) {
      var o = a ? pe(r.buffer) : r.buffer;
      return new r.constructor(o, r.byteOffset, r.length);
    }
    function Dr(r, a) {
      var o = -1, h = r.length;
      for (a || (a = Array(h)); ++o < h; )
        a[o] = r[o];
      return a;
    }
    function tt(r, a, o, h) {
      o || (o = {});
      for (var y = -1, w = a.length; ++y < w; ) {
        var D = a[y], I = void 0;
        et(o, D, I === void 0 ? r[D] : I);
      }
      return o;
    }
    function xr(r, a) {
      return tt(r, rt(r), a);
    }
    function Pr(r) {
      return wr(r, be, rt);
    }
    function te(r, a) {
      var o = r.__data__;
      return Ur(a) ? o[typeof a == "string" ? "string" : "hash"] : o.map;
    }
    function $(r, a) {
      var o = kt(r, a);
      return vr(o) ? o : void 0;
    }
    var rt = Xe ? ue(Xe, Object) : Wr, N = Ir;
    (de && N(new de(new ArrayBuffer(1))) != X || H && N(new H()) != j || ge && N(ge.resolve()) != Me || _e && N(new _e()) != J || fe && N(new fe()) != he) && (N = function(r) {
      var a = Z.call(r), o = a == ce ? r.constructor : void 0, h = o ? k(o) : void 0;
      if (h)
        switch (h) {
          case Wt:
            return X;
          case Vt:
            return j;
          case Kt:
            return Me;
          case Qt:
            return J;
          case Jt:
            return he;
        }
      return a;
    });
    function Lr(r) {
      var a = r.length, o = r.constructor(a);
      return a && typeof r[0] == "string" && x.call(r, "index") && (o.index = r.index, o.input = r.input), o;
    }
    function Nr(r) {
      return typeof r.constructor == "function" && !nt(r) ? br(zt(r)) : {};
    }
    function kr(r, a, o, h) {
      var y = r.constructor;
      switch (a) {
        case Oe:
          return pe(r);
        case d:
        case f:
          return new y(+r);
        case X:
          return Rr(r, h);
        case De:
        case xe:
        case Pe:
        case Le:
        case Ne:
        case ke:
        case qe:
        case Ue:
        case je:
          return Or(r, h);
        case j:
          return Mr(r, h, o);
        case Re:
        case Ce:
          return new y(r);
        case Ae:
          return Ar(r);
        case J:
          return Cr(r, h, o);
        case Ee:
          return Er(r);
      }
    }
    function qr(r, a) {
      return a = a ?? s, !!a && (typeof r == "number" || At.test(r)) && r > -1 && r % 1 == 0 && r < a;
    }
    function Ur(r) {
      var a = typeof r;
      return a == "string" || a == "number" || a == "symbol" || a == "boolean" ? r !== "__proto__" : r === null;
    }
    function jr(r) {
      return !!We && We in r;
    }
    function nt(r) {
      var a = r && r.constructor, o = typeof a == "function" && a.prototype || Y;
      return r === o;
    }
    function k(r) {
      if (r != null) {
        try {
          return Ve.call(r);
        } catch {
        }
        try {
          return r + "";
        } catch {
        }
      }
      return "";
    }
    function zr(r) {
      return me(r, !0, !0);
    }
    function it(r, a) {
      return r === a || r !== r && a !== a;
    }
    function Br(r) {
      return $r(r) && x.call(r, "callee") && (!$t.call(r, "callee") || Z.call(r) == c);
    }
    var ye = Array.isArray;
    function st(r) {
      return r != null && Hr(r.length) && !at(r);
    }
    function $r(r) {
      return Gr(r) && st(r);
    }
    var Fr = Ht || Vr;
    function at(r) {
      var a = re(r) ? Z.call(r) : "";
      return a == _ || a == b;
    }
    function Hr(r) {
      return typeof r == "number" && r > -1 && r % 1 == 0 && r <= s;
    }
    function re(r) {
      var a = typeof r;
      return !!r && (a == "object" || a == "function");
    }
    function Gr(r) {
      return !!r && typeof r == "object";
    }
    function be(r) {
      return st(r) ? pr(r) : Sr(r);
    }
    function Wr() {
      return [];
    }
    function Vr() {
      return !1;
    }
    i.exports = zr;
  }(V, V.exports)), V.exports;
}
var ln = un();
const dn = /* @__PURE__ */ hn(ln), gn = (i) => `${i}::$chat`, ae = "0.7.0", _n = `chat-js/${ae}`, fn = `chat-react/${ae}`, It = ["PUBLISH", "SUBSCRIBE"], mn = {
  params: { agent: _n },
  attachOnSubscribe: !1,
  modes: It
}, pn = {
  params: { agent: fn },
  attachOnSubscribe: !1,
  modes: It
};
class yn {
  constructor(e, t, n, s) {
    n.trace("ChannelManager();", { isReact: s }), this._realtime = t, this._logger = n, this._isReact = s, this._registeredOptions = this._defaultChannelOptions(), this._channelId = gn(e);
  }
  mergeOptions(e) {
    if (this._logger.trace("ChannelManager.mergeOptions();"), this._resolvedChannel)
      throw this._logger.error("channel options cannot be modified after the channel has been requested"), new l.ErrorInfo("channel options cannot be modified after the channel has been requested", 4e4, 400);
    this._registeredOptions = e(this._registeredOptions);
  }
  get() {
    return this._logger.trace("ChannelManager.get();"), this._resolvedChannel ?? (this._resolvedChannel = this._realtime.channels.get(this._channelId, this._registeredOptions)), this._resolvedChannel;
  }
  release() {
    this._logger.trace("ChannelManager.release();", { channelId: this._channelId }), this._resolvedChannel && this._realtime.channels.release(this._channelId);
  }
  _defaultChannelOptions() {
    this._logger.trace("ChannelManager._defaultChannelOptions();");
    const e = this._isReact ? pn : mn;
    this._logger.trace(this._isReact ? "using react channel options" : "using default channel options");
    const t = e.modes ?? [];
    return { ...e, modes: [...t] };
  }
}
class ft {
  /**
   * Constructs a new `DefaultOccupancy` instance.
   * @param roomId The unique identifier of the room.
   * @param channel An instance of the Realtime channel.
   * @param chatApi An instance of the ChatApi.
   * @param logger An instance of the Logger.
   * @param roomOptions The room options.
   */
  constructor(e, t, n, s, c) {
    this._emitter = new C(), this._roomId = e, this._channel = t, this._chatApi = n, this._logger = s, this._roomOptions = c, this._applyChannelSubscriptions();
  }
  /**
   * Sets up channel subscriptions for occupancy.
   */
  _applyChannelSubscriptions() {
    this._channel.subscribe(["[meta]occupancy"], this._internalOccupancyListener.bind(this));
  }
  /**
   * @inheritdoc Occupancy
   */
  subscribe(e) {
    if (this._logger.trace("Occupancy.subscribe();"), !this._roomOptions.occupancy.enableEvents)
      throw new l.ErrorInfo(
        "cannot subscribe to occupancy; occupancy events are not enabled in room options",
        4e4,
        400
      );
    const t = M(e);
    return this._emitter.on(t), {
      unsubscribe: () => {
        this._logger.trace("Occupancy.unsubscribe();"), this._emitter.off(t);
      }
    };
  }
  /**
   * @inheritdoc Occupancy
   */
  unsubscribeAll() {
    this._logger.trace("Occupancy.unsubscribeAll();"), this._emitter.off();
  }
  /**
   * @inheritdoc Occupancy
   */
  async get() {
    return this._logger.trace("Occupancy.get();"), this._chatApi.getOccupancy(this._roomId);
  }
  /**
   * An internal listener that listens for occupancy events from the underlying channel and translates them into
   * occupancy events for the public API.
   */
  _internalOccupancyListener(e) {
    if (this._logger.trace("Occupancy._internalOccupancyListener();", e), typeof e.data != "object") {
      this._logger.error(
        "Occupancy._internalOccupancyListener(); invalid occupancy event received; data is not an object",
        e
      );
      return;
    }
    const { metrics: t } = e.data;
    if (t === void 0) {
      this._logger.error(
        "Occupancy._internalOccupancyListener(); invalid occupancy event received; metrics is missing",
        e
      );
      return;
    }
    const { connections: n, presenceMembers: s } = t;
    if (n === void 0) {
      this._logger.error(
        "Occupancy._internalOccupancyListener(); invalid occupancy event received; connections is missing",
        e
      );
      return;
    }
    if (!Number.isInteger(n)) {
      this._logger.error(
        "Occupancy._internalOccupancyListener(); invalid occupancy event received; connections is not a number",
        e
      );
      return;
    }
    if (s === void 0) {
      this._logger.error(
        "Occupancy._internalOccupancyListener(); invalid occupancy event received; presenceMembers is missing",
        e
      );
      return;
    }
    if (!Number.isInteger(s)) {
      this._logger.error(
        "Occupancy._internalOccupancyListener(); invalid occupancy event received; presenceMembers is not a number",
        e
      );
      return;
    }
    this._emitter.emit("occupancy", {
      connections: n,
      presenceMembers: s
    });
  }
  /**
   * Merges the channel options for the room with the ones required for occupancy.
   *
   * @returns A function that merges the channel options for the room with the ones required for occupancy.
   */
  static channelOptionMerger(e) {
    return (t) => e.occupancy.enableEvents ? { ...t, params: { ...t.params, occupancy: "metrics" } } : t;
  }
}
class mt {
  /**
   * Constructs a new `DefaultPresence` instance.
   * @param channel The Realtime channel instance.
   * @param clientId The client ID, attached to presences messages as an identifier of the sender.
   * A channel can have multiple connections using the same clientId.
   * @param logger An instance of the Logger.
   * @param options The room options.
   */
  constructor(e, t, n, s) {
    this._emitter = new C(), this.subscribeToEvents = (c) => {
      var u;
      try {
        this._emitter.emit(c.action, {
          action: c.action,
          clientId: c.clientId,
          timestamp: c.timestamp,
          // eslint-disable-next-line @typescript-eslint/no-unsafe-member-access
          data: (u = c.data) == null ? void 0 : u.userCustomData
        });
      } catch (d) {
        throw this._logger.error("unable to handle presence event: not a valid presence event", { action: c.action }), new l.ErrorInfo(
          `unable to handle ${c.action} presence event: not a valid presence event`,
          5e4,
          500,
          d.message
        );
      }
    }, this._channel = e, this._clientId = t, this._logger = n, this._options = s, this._applyChannelSubscriptions();
  }
  /**
   * Sets up channel subscriptions for presence.
   */
  _applyChannelSubscriptions() {
    this._channel.presence.subscribe(this.subscribeToEvents.bind(this));
  }
  /**
   * @inheritDoc
   */
  async get(e) {
    return this._logger.trace("Presence.get()", { params: e }), (await this._channel.presence.get(e)).map((n) => {
      var s;
      return {
        clientId: n.clientId,
        action: n.action,
        // eslint-disable-next-line @typescript-eslint/no-unsafe-member-access
        data: (s = n.data) == null ? void 0 : s.userCustomData,
        updatedAt: n.timestamp,
        // eslint-disable-next-line @typescript-eslint/no-unsafe-assignment
        extras: n.extras
      };
    });
  }
  /**
   * @inheritDoc
   */
  async isUserPresent(e) {
    return (await this._channel.presence.get({ clientId: e })).length > 0;
  }
  /**
   * @inheritDoc
   */
  async enter(e) {
    this._logger.trace("Presence.enter()", { data: e });
    const t = {
      userCustomData: e
    };
    return this._channel.presence.enterClient(this._clientId, t);
  }
  /**
   * @inheritDoc
   */
  async update(e) {
    this._logger.trace("Presence.update()", { data: e });
    const t = {
      userCustomData: e
    };
    return this._channel.presence.updateClient(this._clientId, t);
  }
  /**
   * @inheritDoc
   */
  async leave(e) {
    this._logger.trace("Presence.leave()", { data: e });
    const t = {
      userCustomData: e
    };
    return this._channel.presence.leaveClient(this._clientId, t);
  }
  subscribe(e, t) {
    if (this._logger.trace("Presence.subscribe(); listenerOrEvents", { listenerOrEvents: e }), !this._options.presence.enableEvents)
      throw this._logger.error("could not subscribe to presence; presence events are not enabled"), new l.ErrorInfo("could not subscribe to presence; presence events are not enabled", 4e4, 400);
    if (!e && !t)
      throw this._logger.error("could not subscribe to presence; invalid arguments"), new l.ErrorInfo("could not subscribe listener: invalid arguments", 4e4, 400);
    if (t) {
      const n = M(t);
      return this._emitter.on(e, n), {
        unsubscribe: () => {
          this._logger.trace("Presence.unsubscribe();", { events: e }), this._emitter.off(n);
        }
      };
    } else {
      const n = M(e);
      return this._emitter.on(n), {
        unsubscribe: () => {
          this._logger.trace("Presence.unsubscribe();"), this._emitter.off(n);
        }
      };
    }
  }
  /**
   * @inheritDoc
   */
  unsubscribeAll() {
    this._logger.trace("Presence.unsubscribeAll()"), this._emitter.off();
  }
  /**
   * Merges the channel options for the room with the ones required for presence.
   *
   * @param roomOptions The room options to merge for.
   * @returns A function that merges the channel options for the room with the ones required for presence.
   */
  static channelOptionMerger(e) {
    return (t) => (t.modes.includes("PRESENCE") || t.modes.push("PRESENCE"), e.presence.enableEvents && !t.modes.includes("PRESENCE_SUBSCRIBE") && t.modes.push("PRESENCE_SUBSCRIBE"), t);
  }
}
const Se = new Error("request for lock canceled");
var bn = function(i, e, t, n) {
  function s(c) {
    return c instanceof t ? c : new t(function(u) {
      u(c);
    });
  }
  return new (t || (t = Promise))(function(c, u) {
    function d(_) {
      try {
        p(n.next(_));
      } catch (b) {
        u(b);
      }
    }
    function f(_) {
      try {
        p(n.throw(_));
      } catch (b) {
        u(b);
      }
    }
    function p(_) {
      _.done ? c(_.value) : s(_.value).then(d, f);
    }
    p((n = n.apply(i, e || [])).next());
  });
};
class wn {
  constructor(e, t = Se) {
    this._value = e, this._cancelError = t, this._queue = [], this._weightedWaiters = [];
  }
  acquire(e = 1, t = 0) {
    if (e <= 0)
      throw new Error(`invalid weight ${e}: must be positive`);
    return new Promise((n, s) => {
      const c = { resolve: n, reject: s, weight: e, priority: t }, u = vt(this._queue, (d) => t <= d.priority);
      u === -1 && e <= this._value ? this._dispatchItem(c) : this._queue.splice(u + 1, 0, c);
    });
  }
  runExclusive(e) {
    return bn(this, arguments, void 0, function* (t, n = 1, s = 0) {
      const [c, u] = yield this.acquire(n, s);
      try {
        return yield t(c);
      } finally {
        u();
      }
    });
  }
  waitForUnlock(e = 1, t = 0) {
    if (e <= 0)
      throw new Error(`invalid weight ${e}: must be positive`);
    return this._couldLockImmediately(e, t) ? Promise.resolve() : new Promise((n) => {
      this._weightedWaiters[e - 1] || (this._weightedWaiters[e - 1] = []), In(this._weightedWaiters[e - 1], { resolve: n, priority: t });
    });
  }
  isLocked() {
    return this._value <= 0;
  }
  getValue() {
    return this._value;
  }
  setValue(e) {
    this._value = e, this._dispatchQueue();
  }
  release(e = 1) {
    if (e <= 0)
      throw new Error(`invalid weight ${e}: must be positive`);
    this._value += e, this._dispatchQueue();
  }
  cancel() {
    this._queue.forEach((e) => e.reject(this._cancelError)), this._queue = [];
  }
  _dispatchQueue() {
    for (this._drainUnlockWaiters(); this._queue.length > 0 && this._queue[0].weight <= this._value; )
      this._dispatchItem(this._queue.shift()), this._drainUnlockWaiters();
  }
  _dispatchItem(e) {
    const t = this._value;
    this._value -= e.weight, e.resolve([t, this._newReleaser(e.weight)]);
  }
  _newReleaser(e) {
    let t = !1;
    return () => {
      t || (t = !0, this.release(e));
    };
  }
  _drainUnlockWaiters() {
    if (this._queue.length === 0)
      for (let e = this._value; e > 0; e--) {
        const t = this._weightedWaiters[e - 1];
        t && (t.forEach((n) => n.resolve()), this._weightedWaiters[e - 1] = []);
      }
    else {
      const e = this._queue[0].priority;
      for (let t = this._value; t > 0; t--) {
        const n = this._weightedWaiters[t - 1];
        if (!n)
          continue;
        const s = n.findIndex((c) => c.priority <= e);
        (s === -1 ? n : n.splice(0, s)).forEach((c) => c.resolve());
      }
    }
  }
  _couldLockImmediately(e, t) {
    return (this._queue.length === 0 || this._queue[0].priority < t) && e <= this._value;
  }
}
function In(i, e) {
  const t = vt(i, (n) => e.priority <= n.priority);
  i.splice(t + 1, 0, e);
}
function vt(i, e) {
  for (let t = i.length - 1; t >= 0; t--)
    if (e(i[t]))
      return t;
  return -1;
}
var vn = function(i, e, t, n) {
  function s(c) {
    return c instanceof t ? c : new t(function(u) {
      u(c);
    });
  }
  return new (t || (t = Promise))(function(c, u) {
    function d(_) {
      try {
        p(n.next(_));
      } catch (b) {
        u(b);
      }
    }
    function f(_) {
      try {
        p(n.throw(_));
      } catch (b) {
        u(b);
      }
    }
    function p(_) {
      _.done ? c(_.value) : s(_.value).then(d, f);
    }
    p((n = n.apply(i, e || [])).next());
  });
};
class St {
  constructor(e) {
    this._semaphore = new wn(1, e);
  }
  acquire() {
    return vn(this, arguments, void 0, function* (e = 0) {
      const [, t] = yield this._semaphore.acquire(1, e);
      return t;
    });
  }
  runExclusive(e, t = 0) {
    return this._semaphore.runExclusive(() => e(), 1, t);
  }
  isLocked() {
    return this._semaphore.isLocked();
  }
  waitForUnlock(e = 0) {
    return this._semaphore.waitForUnlock(1, e);
  }
  release() {
    this._semaphore.isLocked() && this._semaphore.release();
  }
  cancel() {
    return this._semaphore.cancel();
  }
}
var m = /* @__PURE__ */ ((i) => (i.Initializing = "initializing", i.Initialized = "initialized", i.Attaching = "attaching", i.Attached = "attached", i.Detaching = "detaching", i.Detached = "detached", i.Suspended = "suspended", i.Failed = "failed", i.Releasing = "releasing", i.Released = "released", i))(m || {});
class Sn {
  /**
   * Constructs a new DefaultRoomLifecycle instance.
   * @param roomId The unique identifier of the room.
   * @param logger An instance of the Logger.
   */
  constructor(e, t) {
    this._status = "initialized", this._internalEmitter = new C(), this._emitter = new C(), this._roomId = e, this._logger = t;
  }
  /**
   * @inheritdoc
   */
  get status() {
    return this._status;
  }
  /**
   * @inheritdoc
   */
  get error() {
    return this._error;
  }
  /**
   * @inheritdoc
   */
  onChange(e) {
    const t = M(e);
    return this._emitter.on(t), {
      off: () => {
        this._emitter.off(t);
      }
    };
  }
  onChangeOnce(e) {
    this._internalEmitter.once(e);
  }
  /**
   * @inheritdoc
   */
  offAll() {
    this._emitter.off();
  }
  setStatus(e) {
    const t = {
      current: e.status,
      error: e.error,
      previous: this._status
    };
    this._status = t.current, this._error = t.error, this._logger.info("room status changed", { ...t }), this._internalEmitter.emit(t.current, t), this._emitter.emit(t.current, t);
  }
}
class Tn {
  // CHA-RL7
  constructor(e, t, n, s) {
    this._roomId = e, this._channelManager = t, this._roomLifecycle = n, this._logger = s, this._eventEmitter = new C(), this._hasAttachedOnce = !1, this._isExplicitlyDetached = !1, this._mutex = new St(), this._startMonitoringChannelState(), this._startMonitoringDiscontinuity();
  }
  /**
   * Sets up monitoring of channel state changes to keep room status in sync.
   * If an operation is in progress (attach/detach/release), state changes are ignored.
   * @private
   */
  _startMonitoringChannelState() {
    this._channelManager.get().on((t) => {
      if (this._logger.debug("RoomLifecycleManager.channel state changed", {
        roomId: this._roomId,
        oldState: t.previous,
        newState: t.current,
        reason: t.reason,
        resumed: t.resumed
      }), this._operationInProgress()) {
        this._logger.debug(
          "RoomLifecycleManager._startMonitoringChannelState(); ignoring channel state change - operation in progress",
          {
            roomId: this._roomId,
            roomStatus: this._roomLifecycle.status
          }
        );
        return;
      }
      const n = this._mapChannelStateToRoomStatus(t.current);
      this._setStatus(n, t.reason);
    });
  }
  /**
   * Sets up monitoring for channel discontinuities.
   * A discontinuity exists when an attached or update event comes from the channel with resume=false.
   * The first time we attach, or if we attach after an explicit detach call are not considered discontinuities.
   * @private
   */
  _startMonitoringDiscontinuity() {
    const e = this._channelManager.get();
    e.on("attached", (t) => {
      var n;
      if (!t.resumed && this._hasAttachedOnce && !this._isExplicitlyDetached) {
        const s = new l.ErrorInfo(
          "discontinuity detected",
          U.RoomDiscontinuity,
          ((n = t.reason) == null ? void 0 : n.statusCode) ?? 0,
          t.reason
        );
        this._logger.warn("RoomLifecycleManager._startMonitoringDiscontinuity(); discontinuity detected", {
          roomId: this._roomId,
          error: s
        }), this._eventEmitter.emit(W.Discontinuity, s);
      }
    }), e.on("update", (t) => {
      var n;
      if (!t.resumed && this._hasAttachedOnce && !this._isExplicitlyDetached && t.current === "attached" && t.previous === "attached") {
        const s = new l.ErrorInfo(
          "discontinuity detected",
          U.RoomDiscontinuity,
          ((n = t.reason) == null ? void 0 : n.statusCode) ?? 0,
          t.reason
        );
        this._logger.warn("RoomLifecycleManager._startMonitoringDiscontinuity(); discontinuity detected", {
          roomId: this._roomId,
          error: s
        }), this._eventEmitter.emit(W.Discontinuity, s);
      }
    });
  }
  /**
   * Registers a handler for discontinuity events.
   * @param handler The function to be called when a discontinuity is detected
   * @returns An object with an off() method to deregister the handler
   */
  onDiscontinuity(e) {
    this._logger.trace("RoomLifecycleManager.onDiscontinuity()");
    const t = M(e);
    return this._eventEmitter.on(W.Discontinuity, t), {
      off: () => {
        this._eventEmitter.off(W.Discontinuity, t);
      }
    };
  }
  /**
   * Attaches to the channel and updates room status accordingly.
   * If the room is released/releasing, this operation fails.
   * If already attached, this is a no-op.
   */
  async attach() {
    await this._mutex.runExclusive(
      async () => {
        if (this._logger.trace("RoomLifecycleManager.attach();", { roomId: this._roomId }), this._checkRoomNotReleasing("attach"), this._roomStatusIs(m.Attached)) {
          this._logger.debug("RoomLifecycleManager.attach(); room already attached, no-op", { roomId: this._roomId });
          return;
        }
        const e = this._channelManager.get();
        this._logger.debug("RoomLifecycleManager.attach(); attaching room", {
          roomId: this._roomId,
          channelState: e.state
        });
        try {
          this._setStatus(m.Attaching), await e.attach(), this._setStatus(m.Attached), this._isExplicitlyDetached = !1, this._hasAttachedOnce = !0, this._logger.debug("RoomLifecycleManager.attach(); room attached successfully", { roomId: this._roomId });
        } catch (t) {
          const n = t, s = new l.ErrorInfo(
            `failed to attach room: ${n.message}`,
            n.code,
            n.statusCode,
            n
          ), c = this._mapChannelStateToRoomStatus(e.state);
          throw this._setStatus(c, s), s;
        }
      },
      1
      /* AttachDetach */
    );
  }
  /**
   * Detaches from the channel and updates room status accordingly.
   * If the room is released/releasing, this operation fails.
   * If already detached, this is a no-op.
   */
  async detach() {
    await this._mutex.runExclusive(
      async () => {
        if (this._logger.trace("RoomLifecycleManager.detach();", { roomId: this._roomId }), this._roomStatusIs(m.Failed))
          throw new l.ErrorInfo("cannot detach room, room is in failed state", U.RoomInFailedState, 400);
        if (this._checkRoomNotReleasing("detach"), this._roomStatusIs(m.Detached)) {
          this._logger.debug("RoomLifecycleManager.detach(); room already detached, no-op", { roomId: this._roomId });
          return;
        }
        const e = this._channelManager.get();
        this._logger.debug("RoomLifecycleManager.detach(); detaching room", {
          roomId: this._roomId,
          channelState: e.state
        });
        try {
          this._setStatus(m.Detaching), await e.detach(), this._isExplicitlyDetached = !0, this._setStatus(m.Detached), this._logger.debug("RoomLifecycleManager.detach(); room detached successfully", { roomId: this._roomId });
        } catch (t) {
          const n = t, s = new l.ErrorInfo(
            `failed to detach room: ${n.message}`,
            n.code,
            n.statusCode,
            n
          ), c = this._mapChannelStateToRoomStatus(e.state);
          throw this._setStatus(c, s), s;
        }
      },
      1
      /* AttachDetach */
    );
  }
  /**
   * Releases the room by detaching the channel and releasing it from the channel manager.
   * If the channel is in a failed state, skips the detach operation.
   * Will retry detach until successful unless in failed state.
   */
  async release() {
    await this._mutex.runExclusive(
      async () => {
        if (this._logger.trace("RoomLifecycleManager.release();", { roomId: this._roomId }), this._roomStatusIs(m.Released)) {
          this._logger.debug("RoomLifecycleManager.release(); room already released, no-op", { roomId: this._roomId });
          return;
        }
        if (this._roomStatusIs(m.Initialized) || this._roomStatusIs(m.Detached)) {
          this._logger.debug("RoomLifecycleManager.release(); room is initialized or detached, releasing immediately", {
            roomId: this._roomId,
            status: this._roomLifecycle.status
          }), this._releaseChannel();
          return;
        }
        this._setStatus(m.Releasing);
        const e = this._channelManager.get();
        this._logger.debug("RoomLifecycleManager.release(); attempting channel detach before release", {
          roomId: this._roomId,
          channelState: e.state
        }), await this._channelDetachLoop(e), this._releaseChannel();
      },
      0
      /* Release */
    );
  }
  /**
   * Maps an Ably channel state to a room status
   */
  _mapChannelStateToRoomStatus(e) {
    switch (e) {
      case "initialized":
        return m.Initialized;
      case "attaching":
        return m.Attaching;
      case "attached":
        return m.Attached;
      case "detaching":
        return m.Detaching;
      case "detached":
        return m.Detached;
      case "suspended":
        return m.Suspended;
      case "failed":
        return m.Failed;
      default:
        return this._logger.error("RoomLifecycleManager._mapChannelStateToRoomStatus(); unknown channel state", {
          roomId: this._roomId,
          channelState: e
        }), m.Failed;
    }
  }
  _checkRoomNotReleasing(e) {
    switch (this._roomLifecycle.status) {
      case m.Released:
        throw new l.ErrorInfo(`cannot ${e} room, room is released`, U.RoomIsReleased, 400);
      case m.Releasing:
        throw new l.ErrorInfo(`cannot ${e} room, room is currently releasing`, U.RoomIsReleasing, 400);
    }
  }
  _roomStatusIs(e) {
    return this._roomLifecycle.status === e;
  }
  async _channelDetachLoop(e) {
    for (; ; ) {
      if (e.state === "failed") {
        this._logger.debug("RoomLifecycleManager._channelDetachLoop(); channel is failed, skipping detach", {
          roomId: this._roomId
        });
        break;
      }
      try {
        await e.detach();
        break;
      } catch (n) {
        this._logger.error("RoomLifecycleManager._channelDetachLoop(); failed to detach channel during release", {
          roomId: this._roomId,
          error: n
        }), await new Promise((s) => setTimeout(s, 250));
      }
    }
  }
  _setStatus(e, t) {
    this._logger.debug("RoomLifecycleManager._setStatus(); updating room status", {
      roomId: this._roomId,
      oldStatus: this._roomLifecycle.status,
      newStatus: e,
      hasError: !!t
    }), this._roomLifecycle.setStatus({ status: e, error: t });
  }
  _releaseChannel() {
    this._channelManager.release(), this._setStatus(m.Released), this._logger.debug("RoomLifecycleManager._releaseChannel(); room released successfully", { roomId: this._roomId });
  }
  /**
   * Returns whether there is currently an operation (attach/detach/release) in progress
   * @private
   */
  _operationInProgress() {
    return this._mutex.isLocked();
  }
  testForceHasAttachedOnce(e) {
    this._hasAttachedOnce = e;
  }
}
const oe = {
  /**
   * The default typing options for a chat room.
   */
  typing: {
    /**
     * The default time that a client will wait between sending one typing heartbeat and the next.
     *
     * Spec: CHA-T10.
     */
    heartbeatThrottleMs: 1e4
  },
  /**
   * The default occupancy options for a chat room.
   */
  occupancy: {
    /**
     * Whether to enable occupancy events.
     */
    enableEvents: !1
  },
  /**
   * The default presence options for the room.
   */
  presence: {
    enableEvents: !0
  },
  /**
   * The default options for messages.
   */
  messages: {
    rawMessageReactions: !1,
    defaultMessageReactionType: S.Distinct
  }
}, Rn = (i) => new l.ErrorInfo(`invalid room configuration: ${i}`, 40001, 400), Mn = (i) => {
  An(i.typing);
}, An = (i) => {
  if (i.heartbeatThrottleMs <= 0)
    throw Rn("typing heartbeat interval must be greater than 0");
}, Cn = (i) => ({
  ...oe.typing,
  ...i == null ? void 0 : i.typing
}), En = (i) => ({
  ...oe.occupancy,
  ...i == null ? void 0 : i.occupancy
}), On = (i) => ({
  ...oe.presence,
  ...i == null ? void 0 : i.presence
}), Dn = (i) => ({
  ...oe.messages,
  ...i == null ? void 0 : i.messages
}), xn = (i, e) => ({
  typing: Cn(i),
  occupancy: En(i),
  presence: On(i),
  messages: Dn(i),
  isReactClient: e
});
class Pn {
  constructor(e, t, n, s, c, u) {
    this.type = e, this.clientId = t, this.createdAt = n, this.isSelf = s, this.metadata = c, this.headers = u, Object.freeze(this);
  }
}
function Ln(i, e) {
  var n;
  const t = i;
  if (!t.data)
    throw new l.ErrorInfo("received incoming message without data", 5e4, 500);
  if (!t.data.type || typeof t.data.type != "string")
    throw new l.ErrorInfo("invalid reaction message with no type", 5e4, 500);
  if (!t.clientId)
    throw new l.ErrorInfo("received incoming message without clientId", 5e4, 500);
  if (!t.timestamp)
    throw new l.ErrorInfo("received incoming message without timestamp", 5e4, 500);
  return new Pn(
    t.data.type,
    t.clientId,
    new Date(t.timestamp),
    e ? e === t.clientId : !1,
    t.data.metadata ?? {},
    ((n = t.extras) == null ? void 0 : n.headers) ?? {}
  );
}
const pt = (i, e) => ({
  name: i,
  data: e,
  extras: {
    ephemeral: !0
  }
}), Nn = (i) => {
  const e = i.extras ? i.extras : {};
  return {
    ...i,
    extras: {
      ...e,
      ephemeral: !0
    }
  };
};
class kn {
  /**
   * Constructs a new `DefaultRoomReactions` instance.
   * @param roomId The unique identifier of the room.
   * @param channel The Realtime channel instance.
   * @param clientId The client ID of the user.
   * @param logger An instance of the Logger.
   */
  constructor(e, t, n, s) {
    this._emitter = new C(), this._forwarder = (c) => {
      const u = this._parseNewReaction(c, this._clientId);
      u && this._emitter.emit(se.Reaction, u);
    }, this._roomId = e, this._channel = t, this._clientId = n, this._logger = s, this._applyChannelSubscriptions();
  }
  /**
   * Sets up channel subscriptions for room reactions.
   */
  _applyChannelSubscriptions() {
    this._channel.subscribe([se.Reaction], this._forwarder.bind(this));
  }
  /**
   * @inheritDoc Reactions
   */
  send(e) {
    this._logger.trace("RoomReactions.send();", e);
    const { type: t, metadata: n, headers: s } = e;
    if (!t)
      return Promise.reject(new l.ErrorInfo("unable to send reaction; type not set and it is required", 40001, 400));
    const c = {
      type: t,
      metadata: n ?? {}
    }, u = {
      name: se.Reaction,
      data: c,
      extras: {
        headers: s ?? {}
      }
    };
    return this._channel.publish(Nn(u));
  }
  /**
   * @inheritDoc Reactions
   */
  subscribe(e) {
    this._logger.trace("RoomReactions.subscribe();", { roomId: this._roomId });
    const t = M(e);
    return this._emitter.on(t), {
      unsubscribe: () => {
        this._logger.trace("RoomReactions.unsubscribe();", { roomId: this._roomId }), this._emitter.off(t);
      }
    };
  }
  /**
   * @inheritDoc Reactions
   */
  unsubscribeAll() {
    this._logger.trace("RoomReactions.unsubscribeAll();", { roomId: this._roomId }), this._emitter.off();
  }
  _parseNewReaction(e, t) {
    try {
      return Ln(e, t);
    } catch (n) {
      this._logger.error("failed to parse incoming reaction;", {
        inbound: e,
        error: n,
        roomId: this._roomId
      });
    }
  }
}
class qn extends C {
  /**
   * Constructs a new `DefaultTyping` instance.
   * @param roomId The unique identifier of the room.
   * @param options The options for typing in the room.
   * @param channel The channel for the room.
   * @param clientId The client ID of the user.
   * @param logger An instance of the Logger.
   */
  constructor(e, t, n, s, c) {
    super(), this._timeoutMs = 2e3, this._mutex = new St(), this._internalSubscribeToEvents = (u) => {
      const { name: d, clientId: f } = u;
      if (this._logger.trace("DefaultTyping._internalSubscribeToEvents(); received event", {
        roomId: this._roomId,
        name: d,
        clientId: f
      }), !f) {
        this._logger.error("DefaultTyping._internalSubscribeToEvents(); invalid clientId in received event", {
          roomId: this._roomId,
          inbound: u
        });
        return;
      }
      d === T.Start || d === T.Stop ? this._updateCurrentlyTyping(f, d) : this._logger.warn("DefaultTyping._internalSubscribeToEvents(); unrecognized event", {
        roomId: this._roomId,
        name: d
      });
    }, this._roomId = e, this._clientId = s, this._channel = n, this._heartbeatThrottleMs = t.heartbeatThrottleMs, this._currentlyTyping = /* @__PURE__ */ new Map(), this._logger = c, this._applyChannelSubscriptions();
  }
  /**
   * Sets up channel subscriptions for typing indicators.
   */
  _applyChannelSubscriptions() {
    this._channel.subscribe(
      [T.Start, T.Stop],
      this._internalSubscribeToEvents.bind(this)
    );
  }
  /**
   * CHA-T9
   *
   * @inheritDoc
   */
  get() {
    return this._logger.trace("DefaultTyping.get();", { roomId: this._roomId }), new Set(this._currentlyTyping.keys());
  }
  /**
   * @inheritDoc
   */
  get channel() {
    return this._channel;
  }
  /**
   * Start the heartbeat timer. This will expire after the configured interval.
   */
  _startHeartbeatTimer() {
    if (!this._heartbeatTimerId) {
      this._logger.trace("DefaultTyping.startHeartbeatTimer();", { roomId: this._roomId });
      const e = this._heartbeatTimerId = setTimeout(() => {
        this._logger.debug("DefaultTyping.startHeartbeatTimer(); heartbeat timer expired", { roomId: this._roomId }), e === this._heartbeatTimerId && (this._heartbeatTimerId = void 0);
      }, this._heartbeatThrottleMs);
    }
  }
  /**
   * @inheritDoc
   */
  async keystroke() {
    this._logger.trace("DefaultTyping.keystroke();", { roomId: this._roomId }), this._mutex.cancel(), await this._mutex.acquire().catch((e) => {
      if (e === Se) {
        this._logger.debug("DefaultTyping.keystroke(); mutex was canceled by a later operation", {
          roomId: this._roomId
        });
        return;
      }
      throw new l.ErrorInfo("mutex acquisition failed", 5e4, 500);
    });
    try {
      if (this.channel.state !== "attached" && this.channel.state !== "attaching")
        throw this._logger.error("DefaultTyping.keystroke(); room is not in the correct state ", {
          roomId: this._roomId,
          state: this.channel.state
        }), new l.ErrorInfo("cannot type, room is not in the correct state", 5e4, 500);
      if (this._heartbeatTimerId) {
        this._logger.debug("DefaultTyping.keystroke(); no-op, already typing and heartbeat timer has not expired", {
          roomId: this._roomId
        });
        return;
      }
      await this._channel.publish(pt(T.Start)), this._startHeartbeatTimer(), this._logger.trace("DefaultTyping.keystroke(); starting timers", { roomId: this._roomId });
    } finally {
      this._logger.trace("DefaultTyping.keystroke(); releasing mutex", { roomId: this._roomId }), this._mutex.release();
    }
  }
  /**
   * @inheritDoc
   */
  async stop() {
    this._logger.trace("DefaultTyping.stop();", { roomId: this._roomId }), this._mutex.cancel(), await this._mutex.acquire().catch((e) => {
      if (e === Se) {
        this._logger.debug("DefaultTyping.stop(); mutex was canceled by a later operation", { roomId: this._roomId });
        return;
      }
      throw new l.ErrorInfo("mutex acquisition failed", 5e4, 500);
    });
    try {
      if (this.channel.state !== "attached" && this.channel.state !== "attaching")
        throw this._logger.error("DefaultTyping.stop(); room is not in the correct state ", {
          roomId: this._roomId,
          state: this.channel.state
        }), new l.ErrorInfo("cannot stop typing, room is not in the correct state", 5e4, 500);
      if (!this._heartbeatTimerId) {
        this._logger.debug("DefaultTyping.stop(); no-op, not currently typing", { roomId: this._roomId });
        return;
      }
      await this._channel.publish(pt(T.Stop)), this._logger.trace("DefaultTyping.stop(); clearing timers", { roomId: this._roomId }), clearTimeout(this._heartbeatTimerId), this._heartbeatTimerId = void 0;
    } finally {
      this._logger.trace("DefaultTyping.stop(); releasing mutex", { roomId: this._roomId }), this._mutex.release();
    }
  }
  /**
   * @inheritDoc
   */
  subscribe(e) {
    this._logger.trace("DefaultTyping.subscribe();", { roomId: this._roomId });
    const t = M(e);
    return this.on(t), {
      unsubscribe: () => {
        this._logger.trace("DefaultTyping.unsubscribe();", { roomId: this._roomId }), this.off(t);
      }
    };
  }
  /**
   * @inheritDoc
   */
  unsubscribeAll() {
    this._logger.trace("DefaultTyping.unsubscribeAll();", { roomId: this._roomId }), this.off();
  }
  /**
   * Update the currently typing users. This method is called when a typing event is received.
   * It will also acquire a mutex to ensure that the currentlyTyping state is updated safely.
   * @param clientId The client ID of the user.
   * @param event The typing event.
   */
  _updateCurrentlyTyping(e, t) {
    this._logger.trace("DefaultTyping._updateCurrentlyTyping();", { roomId: this._roomId, clientId: e, event: t }), t === T.Start ? this._handleTypingStart(e) : this._handleTypingStop(e);
  }
  /**
   * Starts a new inactivity timer for the client.
   * This timer will expire after the configured timeout,
   * which is the sum of the heartbeat interval and the inactivity timeout.
   * @param clientId
   */
  _startNewClientInactivityTimer(e) {
    this._logger.trace("DefaultTyping._startNewClientInactivityTimer(); starting new inactivity timer", {
      roomId: this._roomId,
      clientId: e
    });
    const t = setTimeout(() => {
      if (this._logger.trace("DefaultTyping._startNewClientInactivityTimer(); client typing timeout expired", {
        roomId: this._roomId,
        clientId: e
      }), this._currentlyTyping.get(e) !== t) {
        this._logger.debug("DefaultTyping._startNewClientInactivityTimer(); timeout already cleared; ignoring", {
          roomId: this._roomId,
          clientId: e
        });
        return;
      }
      this._currentlyTyping.delete(e), this.emit(q.SetChanged, {
        type: q.SetChanged,
        currentlyTyping: new Set(this._currentlyTyping.keys()),
        change: {
          clientId: e,
          type: T.Stop
        }
      });
    }, this._heartbeatThrottleMs + this._timeoutMs);
    return t;
  }
  /**
   * Handles logic for TypingEventTypes.Start, including starting a new timeout or resetting an existing one.
   * @param clientId
   */
  _handleTypingStart(e) {
    this._logger.debug("DefaultTyping._handleTypingStart();", { roomId: this._roomId, clientId: e });
    const t = this._startNewClientInactivityTimer(e), n = this._currentlyTyping.get(e);
    this._currentlyTyping.set(e, t), n ? (this._logger.debug("DefaultTyping._handleTypingStart(); received heartbeat for currently typing client", {
      roomId: this._roomId,
      clientId: e
    }), clearTimeout(n)) : (this._logger.debug("DefaultTyping._handleTypingStart(); new client started typing", {
      roomId: this._roomId,
      clientId: e
    }), this.emit(q.SetChanged, {
      type: q.SetChanged,
      currentlyTyping: new Set(this._currentlyTyping.keys()),
      change: {
        clientId: e,
        type: T.Start
      }
    }));
  }
  /**
   * Handles logic for TypingEventTypes.Stop, including clearing the timeout for the client.
   * @param clientId
   * @private
   */
  _handleTypingStop(e) {
    const t = this._currentlyTyping.get(e);
    if (!t) {
      this._logger.trace(
        'DefaultTyping._handleTypingStop(); received "Stop" event for client not in currentlyTyping list',
        { roomId: this._roomId, clientId: e }
      );
      return;
    }
    this._logger.debug("DefaultTyping._handleTypingStop(); client stopped typing", { roomId: this._roomId, clientId: e }), clearTimeout(t), this._currentlyTyping.delete(e), this.emit(q.SetChanged, {
      type: q.SetChanged,
      currentlyTyping: new Set(this._currentlyTyping.keys()),
      change: {
        clientId: e,
        type: T.Stop
      }
    });
  }
  get heartbeatThrottleMs() {
    return this._heartbeatThrottleMs;
  }
}
class Un {
  /**
   * Constructs a new Room instance.
   *
   * @param roomId The unique identifier of the room.
   * @param nonce A random identifier for the room instance, useful in debugging and logging.
   * @param options The options for the room.
   * @param realtime An instance of the Ably Realtime client.
   * @param chatApi An instance of the ChatApi.
   * @param logger An instance of the Logger.
   */
  constructor(e, t, n, s, c, u) {
    Mn(n), this._nonce = t, this._logger = u.withContext({ roomId: e, roomNonce: t }), this._logger.debug("Room();", { options: n }), this._roomId = e, this._options = n, this._chatApi = c, this._lifecycle = new Sn(e, this._logger);
    const d = this._channelManager = this._getChannelManager(n, s, this._logger), f = d.get();
    this._messages = new an(
      e,
      n.messages,
      f,
      this._chatApi,
      s.auth.clientId,
      this._logger
    ), this._presence = new mt(f, s.auth.clientId, this._logger, n), this._typing = new qn(e, n.typing, f, s.auth.clientId, this._logger), this._reactions = new kn(e, f, s.auth.clientId, this._logger), this._occupancy = new ft(e, f, this._chatApi, this._logger, n), this._lifecycleManager = new Tn(e, d, this._lifecycle, this._logger);
    let p = !1;
    this._finalizer = async () => {
      if (p) {
        this._logger.debug("Room.finalizer(); already finalized");
        return;
      }
      await this._lifecycleManager.release(), p = !0;
    };
  }
  /**
   * Gets the channel manager for the room, which handles merging channel options together and creating channels.
   *
   * @param options The room options.
   * @param realtime  An instance of the Ably Realtime client.
   * @param logger An instance of the Logger.
   */
  _getChannelManager(e, t, n) {
    const s = new yn(this.roomId, t, n, e.isReactClient);
    return s.mergeOptions(ft.channelOptionMerger(e)), s.mergeOptions(mt.channelOptionMerger(e)), s.mergeOptions(wt.channelOptionMerger(e)), s;
  }
  /**
   * @inheritdoc Room
   */
  get roomId() {
    return this._roomId;
  }
  /**
   * @inheritDoc Room
   */
  options() {
    return dn(this._options);
  }
  /**
   * @inheritdoc Room
   */
  get messages() {
    return this._messages;
  }
  /**
   * @inheritdoc Room
   */
  get presence() {
    return this._presence;
  }
  /**
   * @inheritdoc Room
   */
  get reactions() {
    return this._reactions;
  }
  /**
   * @inheritdoc Room
   */
  get typing() {
    return this._typing;
  }
  /**
   * @inheritdoc Room
   */
  get occupancy() {
    return this._occupancy;
  }
  /**
   * @inheritdoc Room
   */
  get status() {
    return this._lifecycle.status;
  }
  /**
   * @inheritdoc Room
   */
  get error() {
    return this._lifecycle.error;
  }
  /**
   * @inheritdoc Room
   */
  onStatusChange(e) {
    return this._lifecycle.onChange(e);
  }
  /**
   * @inheritdoc Room
   */
  offAllStatusChange() {
    this._lifecycle.offAll();
  }
  /**
   * @inheritdoc Room
   */
  async attach() {
    return this._logger.trace("Room.attach();"), this._lifecycleManager.attach();
  }
  /**
   * @inheritdoc Room
   */
  async detach() {
    return this._logger.trace("Room.detach();"), this._lifecycleManager.detach();
  }
  /**
   * Releases resources associated with the room.
   * We guarantee that this does not throw an error.
   */
  release() {
    return this._logger.trace("Room.release();"), this._finalizer();
  }
  /**
   * A random identifier for the room instance, useful in debugging and logging.
   *
   * @returns The nonce.
   */
  get nonce() {
    return this._nonce;
  }
  /**
   * @internal
   *
   * Returns the rooms lifecycle.
   */
  get lifecycle() {
    return this._lifecycle;
  }
  /**
   * @internal
   */
  get lifecycleManager() {
    return this._lifecycleManager;
  }
  /**
   * @inheritdoc Room
   */
  onDiscontinuity(e) {
    return this._logger.trace("Room.onDiscontinuity();", { nonce: this._nonce, roomId: this._roomId }), this._lifecycleManager.onDiscontinuity(e);
  }
  /**
   * @inheritdoc Room
   */
  get channel() {
    return this._channelManager.get();
  }
}
class jn {
  /**
   * Constructs a new Rooms instance.
   *
   * @param realtime An instance of the Ably Realtime client.
   * @param clientOptions The client options from the chat instance.
   * @param logger An instance of the Logger.
   */
  constructor(e, t, n) {
    this._rooms = /* @__PURE__ */ new Map(), this._releasing = /* @__PURE__ */ new Map(), this._isReact = !1, this._realtime = e, this._chatApi = new on(e, n), this._clientOptions = t, this._logger = n;
  }
  /**
   * @inheritDoc
   */
  get(e, t) {
    this._logger.trace("Rooms.get();", { roomId: e });
    const n = this._rooms.get(e);
    if (n)
      return K(n.options, t) ? (this._logger.debug("Rooms.get(); returning existing room", { roomId: e, nonce: n.nonce }), n.promise) : Promise.reject(new l.ErrorInfo("room already exists with different options", 4e4, 400));
    const s = this._releasing.get(e), c = cn();
    if (!s) {
      const f = this._makeRoom(e, c, t), p = {
        promise: Promise.resolve(f),
        nonce: c,
        options: t
      };
      return this._rooms.set(e, p), this._logger.debug("Rooms.get(); returning new room", { roomId: e, nonce: f.nonce }), p.promise;
    }
    const u = new AbortController(), d = new Promise((f, p) => {
      const _ = () => {
        this._logger.debug("Rooms.get(); aborted before init", { roomId: e }), p(
          new l.ErrorInfo(
            "room released before get operation could complete",
            U.RoomReleasedBeforeOperationCompleted,
            400
          )
        );
      };
      u.signal.addEventListener("abort", _), s.then(() => {
        if (u.signal.aborted) {
          this._logger.debug("Rooms.get(); aborted before releasing promise resolved", { roomId: e });
          return;
        }
        this._logger.debug("Rooms.get(); releasing finished", { roomId: e });
        const b = this._makeRoom(e, c, t);
        u.signal.removeEventListener("abort", _), f(b);
      }).catch((b) => {
        p(b);
      });
    });
    return this._rooms.set(e, {
      promise: d,
      options: t,
      nonce: c,
      abort: u
    }), this._logger.debug("Rooms.get(); creating new promise dependent on previous release", { roomId: e }), d;
  }
  /**
   * @inheritDoc
   */
  get clientOptions() {
    return this._clientOptions;
  }
  /**
   * @inheritDoc
   */
  release(e) {
    this._logger.trace("Rooms.release();", { roomId: e });
    const t = this._rooms.get(e), n = this._releasing.get(e);
    if (!t)
      return n ? (this._logger.debug("Rooms.release(); waiting for previous release call", {
        roomId: e
      }), n) : (this._logger.debug("Rooms.release(); room does not exist", { roomId: e }), Promise.resolve());
    if (n)
      return t.abort && (this._logger.debug("Rooms.release(); aborting get call", { roomId: e, existingNonce: t.nonce }), t.abort.abort(), this._rooms.delete(e)), n;
    this._rooms.delete(e);
    const s = t.promise.then((c) => (this._logger.debug("Rooms.release(); releasing room", { roomId: e, nonce: t.nonce }), c.release().then(() => {
      this._logger.debug("Rooms.release(); room released", { roomId: e, nonce: t.nonce }), this._releasing.delete(e);
    })));
    return this._logger.debug("Rooms.release(); creating new release promise", { roomId: e, nonce: t.nonce }), this._releasing.set(e, s), s;
  }
  /**
   * makes a new room object
   *
   * @param roomId The ID of the room.
   * @param nonce A random, internal identifier useful for debugging and logging.
   * @param options The options for the room.
   *
   * @returns DefaultRoom A new room object.
   */
  _makeRoom(e, t, n) {
    return new Un(
      e,
      t,
      xn(n, this._isReact),
      this._realtime,
      this._chatApi,
      this._logger
    );
  }
  /**
   * Sets react JS mode.
   */
  useReact() {
    this._logger.trace("Rooms.useReact();"), this._isReact = !0;
  }
}
class $n {
  /**
   * Constructor for Chat
   * @param realtime - The Ably Realtime client.
   * @param clientOptions - The client options.
   */
  constructor(e, t) {
    this._realtime = e, this._clientOptions = Xr(t), this._logger = Qr(this._clientOptions), this._connection = new tn(e, this._logger), this._rooms = new jn(e, this._clientOptions, this._logger), this._addAgent("chat-js"), this._logger.trace(`ably chat client version ${ae}; initialized`);
  }
  /**
   * Returns the rooms object, which provides access to chat rooms.
   *
   * @returns The rooms object.
   */
  get rooms() {
    return this._rooms;
  }
  /**
   * Returns the underlying connection to Ably, which can be used to monitor the client's
   * connection to Ably servers.
   *
   * @returns The connection object.
   */
  get connection() {
    return this._connection;
  }
  /**
   * Returns the clientId of the current client.
   *
   * @returns The clientId.
   */
  get clientId() {
    return this._realtime.auth.clientId;
  }
  /**
   * Returns the underlying Ably Realtime client.
   * @returns The Ably Realtime client.
   */
  get realtime() {
    return this._realtime;
  }
  /**
   * Returns the resolved client options for the client, including any defaults that have been set.
   * @returns The client options.
   */
  get clientOptions() {
    return this._clientOptions;
  }
  /**
   * Returns the logger instance for the client.
   * @internal
   * @returns The logger instance.
   */
  get logger() {
    return this._logger;
  }
  /**
   * Adds additional agent information to the client.
   * Used internally to add React-specific agent information.
   * @internal
   */
  addReactAgent() {
    this._addAgent("chat-react"), this._rooms.useReact();
  }
  /**
   * Sets the agent string for the client.
   * @param agent - The agent to add.
   * @internal
   */
  _addAgent(e) {
    const t = this._realtime;
    t.options.agents = { ...t.options.agents ?? t.options.agents, [e]: ae };
  }
}
export {
  $n as ChatClient,
  v as ChatMessageActions,
  en as ConnectionStatus,
  U as ErrorCodes,
  yt as LogLevel,
  P as MessageEvents,
  S as MessageReactionType,
  ve as OrderBy,
  rn as PresenceEvents,
  m as RoomStatus,
  T as TypingEventTypes,
  q as TypingSetEventTypes,
  Bn as errorInfoIs
};
//# sourceMappingURL=ably-chat.js.map
